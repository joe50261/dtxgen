# dtxgen web

純前端 DTX 自動製譜 — 詳見 `app/README_WEB.md`。

```bash

npm install            # 會自動抓 demucs(含分離模型)並同步到 app/
npm start              # 開 http://localhost:8080(或 npm run serve)
npm test               # 冒煙測試(node)
```
